export default function AdminInquiries() {
  return (
    <div>
      <h1 className="font-display text-2xl font-semibold text-[var(--text-primary)]">Inquiries</h1>
      <p className="mt-1 text-sm text-[var(--text-secondary)]">
        CRUD management UI for Inquiries connects to the backend admin API — scaffolded, not yet wired in this foundation pass.
      </p>
    </div>
  )
}
